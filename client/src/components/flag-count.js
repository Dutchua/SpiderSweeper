const FlagCount = ({ count }) => {
  return `    <section>
      <img class="icon" src="./public/icons/newspaper.svg" alt="icon" />
      <label> ${count}</label>
    </section>`;
};

export default FlagCount;
